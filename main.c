#include <stdio.h>
#include <menu.h>

int main(int argc, char const *argv[]) {
  int opcao;

// Nicoleti é boi
  /* Aqui fica a função de login e loading de dados!*/


  for(;;){
    system("cls");
    opcao=menu();
    switch (opcao) {
      case /* value */:
    }
  }
  return 0;
}
